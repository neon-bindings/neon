---
title: Install Neon
description: Install the Rust toolchain and Node.js prerequisites, then scaffold a new project with npm init neon@latest.
---

:::caution[Status: skeleton]
This page is a placeholder. Content forthcoming.
:::

This guide covers the prerequisites for building a Neon module: a supported version of Node.js, the Rust toolchain via `rustup`, and any platform-specific build tools. Once those are in place, `npm init neon@latest` scaffolds a new project ready to build and load from JavaScript.
