---
title: CLI reference
description: Command-line tools shipped with Neon, including create-neon and cargo-cp-artifact.
status: todo
---

This reference documents the command-line tools that ship with Neon: `create-neon` (the project scaffolder invoked by `npm init neon@latest`) and `cargo-cp-artifact` (the build-output mover used in the generated `package.json` scripts). Each tool's flags and expected usage are listed here.
