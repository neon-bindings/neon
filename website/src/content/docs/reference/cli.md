---
title: CLI reference
description: Command-line tools shipped with Neon, including create-neon and the neon CLI.
status: todo
---

This reference documents the command-line tools that ship with Neon: `create-neon` (the project scaffolder invoked by `npm init neon@latest`) and the `neon` CLI from `@neon-rs/cli` (used by the generated `package.json` scripts, e.g. `neon dist`). Each tool's flags and expected usage are listed here.
