[package]
name = "tests"
version = "0.1.0"
authors = ["The Neon Community"]
license = "MIT/Apache-2.0"
build = "build.rs"

[lib]
name = "tests"
crate-type = ["dylib"]

[build-dependencies]
neon-build = {version = "0.1.11", path = "../../crates/neon-build"}

[dependencies]
neon = {path = "../../"}
