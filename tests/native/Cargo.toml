[package]
name = "tests"
version = "0.1.0"
authors = ["The Neon Community"]
license = "MIT/Apache-2.0"
build = "build.rs"

[lib]
name = "tests"
crate-type = ["dylib"]

[dev-dependencies]
neon-build = "0.1.11"

[dependencies]
neon = {path = "../../"}
