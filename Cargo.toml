[package]
name = "neon"
version = "0.0.9"
authors = ["Dave Herman <dherman@mozilla.com>"]
description = "A safe abstraction layer for Node.js."
repository = "https://github.com/dherman/neon"
license = "MIT"
exclude = ["neon.jpg"]

[dependencies]
neon-sys = { version = "0.0.6", path = "crates/neon-sys" }
